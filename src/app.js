import React from 'react';
import ReactDOM from 'react-dom';
import BookmarkFormContainer from './BookmarkFormContainer.jsx'

const container = document.querySelector('#container');
ReactDOM.render(<BookmarkFormContainer />, container);